# ReeF Bot (Cybork)

A Discord bot with economy, music, moderation, and welcome features.

## Running the bot

The bot starts with `node index.js`. It requires the following secrets to be set:

- `DISCORD_TOKEN` — Your Discord bot token (required)
- `DATABASE_URL` — PostgreSQL connection string (auto-provided by Replit)
- `TOPGG_API` — Top.gg API key (optional, for stats posting)

## User preferences

- Keep the existing project structure (commands/, events/, handlers/, SlashCommands/, util/)
